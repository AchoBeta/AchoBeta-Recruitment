server:
  port: 9002

--- # 数据源配置
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1/recruitment?serverTimezone=UTC
    username: root
    password: 123456
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver

###### 中间件后续迁移到 nacos 上 ######

--- # redis
spring.data:
  redis:
    # 地址
    host: http://1.12.44.120
    # 端口
    port: 6379
    # 数据库索引
    database: 0
    # 密码(如没有密码请注释掉)
    # password:
    # 连接超时时间
    connect-timeout: 10s
    # 是否开启ssl
    ssl.enabled: false